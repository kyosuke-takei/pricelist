name: Daily Scrape (DISABLED)
on:
  # スクレイピングを停止しています
  # schedule:
  #   - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:
    inputs:
      confirm:
        description: 'この workflow は現在無効化されています'
        required: true
        default: 'disabled'
jobs:
  scrape:
    runs-on: ubuntu-latest
    if: false  # 手動実行も含め、すべての実行をブロック
    steps:
      - name: Disabled
        run: echo "Scraping is disabled"
